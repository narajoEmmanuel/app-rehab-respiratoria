/**
 * Purpose: Expo Router login route wrapper.
 * Module: app routing
 * Dependencies: auth module screen
 * Notes: Thin routing layer only.
 */
import { LoginScreen } from '@/src/modules/auth/screens/LoginScreen';

export default LoginScreen;

