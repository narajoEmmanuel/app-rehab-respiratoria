/**
 * Dev route: /dev/respira-bunny-image-showcase
 * Validates RespiraBunnyImage poses before integrating pop-ups / onboarding.
 */

import { RespiraBunnyImageShowcase } from '@/src/shared/ui/RespiraBunnyImageShowcase';

export default function RespiraBunnyImageShowcaseRoute() {
  return <RespiraBunnyImageShowcase />;
}
