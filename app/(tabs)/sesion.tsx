/**
 * Purpose: Expo Router session route wrapper.
 * Module: app routing
 * Dependencies: session module screen
 * Notes: Thin routing layer only.
 */
import { SessionScreen } from '@/src/modules/session/screens/SessionScreen';

export default SessionScreen;

