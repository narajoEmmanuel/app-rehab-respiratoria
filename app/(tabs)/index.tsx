/**
 * Purpose: Expo Router home route wrapper.
 * Module: app routing
 * Dependencies: home module screen
 * Notes: Keep route files thin; domain code lives in src/modules.
 */
import { HomeScreen } from '@/src/modules/home/screens/HomeScreen';

export default HomeScreen;
