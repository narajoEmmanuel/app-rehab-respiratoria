/**
 * Purpose: Placeholder for ESP32 WebSocket transport over local WiFi.
 * Module: device
 * Dependencies: none
 * Notes: Real connection, reconnect, and message handling will live here in a later phase.
 *        SensorConnectionScreen and related flows still use local mocks / demo mode until then.
 */

export const websocketTransportPlaceholder = {
  /** Set true when a real socket client replaces this scaffold. */
  implemented: false as const,
};
