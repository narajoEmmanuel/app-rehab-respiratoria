/**
 * Purpose: Placeholder for respiratory data ingestion.
 * Module: device
 * Dependencies: device/types
 * Notes: Future parser/validator layer for device payloads.
 */
import type { RespiratorySample } from '@/src/modules/device/types';

export function ingestDeviceSample(sample: RespiratorySample): RespiratorySample {
  return sample;
}

