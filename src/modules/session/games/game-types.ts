/**
 * Purpose: Identifiers for visual game implementations only.
 * Module: session/games
 * Dependencies: none
 * Notes: Do not encode difficulty here; map levels in session/registry.
 */

/** Visual minigame implementation the UI can mount by id. */
export type GameVisualId = 'visual-placeholder' | 'monkey-jungle' | 'rocket-experimental';
